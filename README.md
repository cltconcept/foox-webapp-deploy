# Foox webapp — bundle de déploiement

Sortie `next build` standalone de apps/web (monorepo privé). Aucune source,
aucun secret : la configuration vient des variables d'environnement Coolify.
Régénéré à chaque déploiement.
